/*
* Put here the requests to the DataSystem.
*/

americano = require('americano');

module.exports = {
    geolocationlog: {
        all: americano.defaultRequests.all
    },
    
    identity: {
        all: americano.defaultRequests.all
        
    }    
};
